---
title: "Week 4 - Data cleaning and processing"
author: "Erika Siregar"
format:
  live-revealjs:
    theme: slides.scss
    title-slide-attributes:
      data-background-color: "#9cbbbd"
    transition: slide
    slide-number: true
    countdown:
      color_background: "#283c4d"
      color_text: "#ffffff"
resources:
  - ../data
execute:
  echo: true
  warning: false
webr:
  render-df: gt-interactive
---

{{< include ../../../_extensions/r-wasm/live/_knitr.qmd >}}

# Learning objectives {.smaller}

Over the past three weeks we've looked at R basics, data structures, importing and basic visualisation. This week focuses on data transformation using the tidyverse, mainly dplyr.

::: {.columns}
::: {.column width="70%"}
1. Manipulate data with the dplyr package

2. Join two datasets (tables) using a common key

3. Spot issues in real datasets (outliers, missing values) and handle them

:::
::: {.column width="5%"}
:::
::: {.column width="25%"}
{{< qrcode https://erikaris.github.io/uos-gta/intro-to-data-science/autumn-2025/slides/week-4.html width=300 height=300 >}}
:::
::: 


# Data transformation with dplyr {.smaller}

dplyr gives concise verbs for common tasks:

- filter() — pick rows
- arrange() — reorder rows
- select() — pick columns
- mutate() — create/transform columns
- summarise() — collapse to summaries


## Inspect mpg {.smaller}

We'll use the built-in mpg dataset for examples.

```{webr}
#| warning: false
#| autorun: true
library(tidyverse)
str(mpg)
```


## filter() - Pick observations {.smaller}

Select Audi cars? Engine displacement > 2?

```{webr}
#| warning: false
#| autorun: true
filter(mpg, manufacturer=="audi")
filter(mpg, displ > 2)
```


## filter() - Multiple conditions {.smaller}

Combine conditions with `&` and `|`, or use `%in%` for membership.

```{webr}
#| warning: false
#| autorun: true
filter(mpg, displ > 2 & cyl > 6)
filter(mpg, manufacturer=="audi", year==1999)
filter(mpg, manufacturer=="audi" & year==1999)
```

## Your turn: Filter multiple condition {.smaller}

::: your-turn
Can you filter rows where the manufacturer is Audi OR the year of production (year) is 1999? Can you filter rows where the year of production (year) is 1999 and the manufacturer is NOT Audi?
:::

::: exercise-folder
```{webr} 
#| exercise: hist-y1-y2
filter(mpg, ___)
```
:::

{{< countdown 10:00 >}}


## arrange() — Reordering rows {.smaller}

```{webr}
#| warning: false
#| autorun: true
rio2016Medals <- read_csv("data/Rio2016.csv")
arrange(rio2016Medals, Country)
```


## arrange() — Descending and multi-column ordering {.smaller}

```{webr}
#| warning: false
#| autorun: true
arrange(rio2016Medals, desc(Country))
arrange(rio2016Medals, desc(Gold), desc(Silver), desc(Bronze))
```


## Your turn: Result piping {.smaller}

::: your-turn
to better view the ordered tibble, pipe the results of the arrange function into the function View. Make sure that ties between countries with the same number of gold and silver medals are sorted based on the number of bronze medals.
:::

::: exercise-folder
```{webr} 
#| exercise: dplyr-pipe-medals
rio2016Medals
```
:::

{{< countdown 10:00 >}}


## select() — Selecting columns {.smaller}

```{webr}
#| warning: false
#| autorun: true
select(mpg, manufacturer, hwy)
```


## select() — Selecting with pattern {.smaller}

```{webr}
#| warning: false
#| autorun: true
select(mpg, starts_with("d"))
```


## Combine select + filter + arrange with pipes {.smaller}

```{webr}
#| warning: false
#| autorun: true
mpg %>% 
  select(manufacturer, hwy) %>% 
  filter(manufacturer != "chevrolet" & hwy >=20) %>%
  arrange(desc(manufacturer))
```


## mutate() — New variables {.smaller}

Create a Total column in the medals data?
Mutate only affects later steps in the pipe unless you assign it to a variable.

```{webr}
#| warning: false
#| autorun: true
mutate(rio2016Medals, Total = Gold + Silver + Bronze)
```


## summarise() {.smaller}

```{webr}
#| warning: false
#| autorun: true
summarise(mpg, avg = mean(hwy))
```


## summarise() and group_by() {.smaller}

```{webr}
#| warning: false
#| autorun: true
group_by(mpg, year, manufacturer) %>% summarise(count = n())
```


## Your turn: Combine mutate and summarise {.smaller}

::: your-turn
Look at the second page of cheatsheet for useful functions to use with mutate and summarise. How many unique models do each manufacturer produce? Create a new column with a ratio of highway efficiency (hwy) vs city efficiency (cty) called HwyCtyRatio.
:::

::: exercise-folder
```{webr} 
#| exercise: dplyr-summarise-hwyctyration
mpg
```
:::

{{< countdown 10:00 >}}


# Combining datasets


## Explore datasets {.smaller}

Use the nycflights13 package to demonstrate joins between relational tables.

```{webr}
#| warning: false
#| autorun: true
library(nycflights13)

nycflights13::airlines
nycflights13::airports
```


## Combining datasets (left join) {.smaller}

Add full airline name to flights

```{webr}
#| warning: false
#| autorun: true
flights2 <- flights %>% select(year:day, hour, origin, dest, tailnum, carrier)
flights2 %>% left_join(airlines, by = "carrier")
```

# Spot issues in real datasets

## Working with real data — Free School Meals (FSM) {.smaller}

```{webr}
#| warning: false
#| autorun: true
meals <- read_csv("data/freeschoolmeals.csv")
meals
```

## FSM - Common issues {.smaller}

- sentinel outlier value: 9999 (represents suppressed values < 5)
- NA values for missing data

Check summary and mean behaviour:

```{webr}
#| warning: false
#| autorun: true
summary(meals$FSMTaken)
mean(meals$FSMTaken)
mean(meals$FSMTaken, na.rm=TRUE)
```


## FSM - Filter out 9999 and handle NA {.smaller}

```{webr}
#| warning: false
#| autorun: true
actualFSMTaken <- meals$FSMTaken[meals$FSMTaken != 9999]
length(actualFSMTaken)
mean(actualFSMTaken, na.rm = TRUE)
```

Or use dplyr to keep NA and exclude 9999:

```{webr}
#| warning: false
#| autorun: true
filter(meals, (FSMTaken < 9999 | is.na(FSMTaken))) %>% count()
filter(meals, (FSMTaken < 9999 | is.na(FSMTaken))) %>% 
  mutate(newFSMTaken = ifelse(is.na(FSMTaken), floor(mean(FSMTaken, na.rm = TRUE)), FSMTaken))
```


## Missing values — simple imputation example {.smaller}

```{webr}
#| warning: false
#| autorun: true
y <- c(4,5,6,NA)
is.na(y)
y[is.na(y)] <- mean(y, na.rm = TRUE)
y
```


## Your turn: Experiment with different values {.smaller}

::: your-turn
Lets imagine someone asks you to obtain the mean of FSMTaken in the worst case scenario, where all the 9999 values (when the value is less than 5) are considered to be 4. Use the functions you have learned today to change those 9999 values to 4 (probably best to create a new column for this) and calculate the mean. Do the same thing, for the case scenario where FSMTaken for those 9999 values is considered to be 0.
:::

::: exercise-folder
```{webr} 
#| exercise: dplyr-summarise-fsmtaken
meals
```
:::

{{< countdown 10:00 >}}


# Further resources {.smaller}

- R for Data Science (Chapters 3, 8, 10)
- RStudio data-wrangling cheatsheet
- Datacamp tutorials and the links embedded in the worksheet


# Important points {.smaller}

- Always inspect your data: summary(), str(), View()
- Watch out for sentinel values (e.g. 9999) and decide how to treat them before computing summaries
- Missing values (NA) must be handled explicitly (na.rm or imputation)
- dplyr verbs (filter, select, arrange, mutate, summarise) + pipes make transformation readable and reproducible
- Use joins carefully: pick the right join (left/inner/full) and check for unexpected NAs after the join
- When sampling or random processes, set a seed to make results reproducible
